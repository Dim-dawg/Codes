# Project

Repository initialized with a minimal README.

## Overview

Contains Python scripts and services.

## Setup

1. Create a virtual environment: `python -m venv venv`
2. Activate it and install dependencies from `requirements.txt` if present.
